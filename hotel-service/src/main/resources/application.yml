server:
  port: 15500
spring:
  profiles:
    active: local
  #  batch:
  #    job:
  #      enabled: false
  servlet:
    multipart:
      max-file-size: 51200KB
      max-request-size: 51200KB
springdoc:
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include:*
  endpoint:
    health:
      show-details: always